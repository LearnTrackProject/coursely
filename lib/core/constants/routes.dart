import 'package:go_router/go_router.dart';

class Routes {
  var route = GoRouter(
    initialLocation: "add initial Screen",
    routes: [
      // GoRoute(path: "path",builder: (context , state){return screen}),
    ],
  );
}
